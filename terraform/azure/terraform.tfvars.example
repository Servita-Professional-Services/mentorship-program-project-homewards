# Copy this file to terraform.tfvars and fill in your values
# terraform.tfvars is gitignored — never commit real credentials

resource_group_name = "rg-virtual-wards-dev"
environment         = "dev"
prefix              = "virtualwards"
database_url        = "postgresql://user:password@host:5432/virtualwards"
